import React, { useState } from 'react';

function UserDataForm() {
  // Definindo as variáveis que vão guardar as informações
  
  const [cintura, setCintura] = useState('');
  const [quadril, setQuadril] = useState('');
  const [dobrasCutaneas, setDobrasCutaneas] = useState({ triceps: '', subescapular: '', axilar: '' });
  const [rcq, setRcq] = useState(null);

  // Função para calcular a relação Cintura-Quadril (RCQ)



}